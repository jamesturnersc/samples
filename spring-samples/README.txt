Some samples of possible ways to do things:

- Using plugin to generate Java classes (using protobuf compiler) and package in jar
- Using Spring Cloud Stream to send and receive those Java classes using Kafka in protobuf format.